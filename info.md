# Дополнительные заметки по Docker

## 🏗 Архитектура Docker

Docker состоит из:

- Docker Client — отправляет команды
- Docker Daemon — выполняет команды
- Docker Hub — хранилище образов

---

## 🔄 Жизненный цикл контейнера

1. Создание контейнера
2. Запуск
3. Остановка
4. Перезапуск
5. Удаление

Команды:

```bash
docker create nginx
docker start <container_id>
docker restart <container_id>
docker stop <container_id>
docker rm <container_id>
```

---

## 🌍 Работа с портами

Проброс портов:

```bash
docker run -p 8080:80 nginx
```

8080 — порт на компьютере  
80 — порт внутри контейнера

---

## 💾 Работа с Volume

Создание volume:

```bash
docker volume create myvolume
```

Запуск контейнера с volume:

```bash
docker run -v myvolume:/data nginx
```

---

## 🌐 Работа с сетью

Создание сети:

```bash
docker network create mynetwork
```

Запуск контейнера в сети:

```bash
docker run --network mynetwork nginx
```

---

## 📦 Docker Compose

Docker Compose используется для запуска нескольких контейнеров.

Пример `docker-compose.yml`:

```yaml
version: "3"
services:
  web:
    image: nginx
    ports:
      - "8080:80"
```

Запуск:

```bash
docker-compose up
```

---

## 📌 Итог

Docker упрощает:
- Разработку
- Тестирование
- Развёртывание приложений
- Работу в команде
